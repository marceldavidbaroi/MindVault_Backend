# Database connection
DATABASE_HOST=localhost
###########################################################
# 🗄️ DATABASE CONFIGURATION
###########################################################

# Database connection settings for PostgreSQL
DATABASE_HOST=localhost           # Hostname or IP of your database server
DATABASE_PORT=5432                # Default PostgreSQL port
DATABASE_USER=postgres            # Database username
DATABASE_PASSWORD=1234            # Database password
DATABASE_NAME=mind_vault          # Name of your application's database


###########################################################
# ⚙️ APPLICATION SETTINGS
###########################################################

# Node environment: 'development', 'staging', or 'production'
NODE_ENV=development

# Port the NestJS server will run on
PORT=3000

# Global API prefix — all routes will start with this (e.g., /api/v1/users)
API_PREFIX=api/v1


###########################################################
# 🌐 CORS CONFIGURATION
###########################################################

# Comma-separated list of frontend origins allowed to call the API
# Example: local frontend and LAN access
CORS_ORIGINS=http://localhost:3001,http://192.168.0.105:3001

# Whether to allow credentials (cookies, auth headers, etc.)
CORS_CREDENTIALS=true


note: create a db mind_vault